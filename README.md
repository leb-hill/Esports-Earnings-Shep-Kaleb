# Esports-Earnings-Shep-Kaleb

### returns games that have had fewer tournaments than the given number
#### chosenTournaments {number} - a number that the function uses to return games with fewer tournaments
#### return {list} - the list of games that have had fewer tournaments 
    
**` function getGamesWithFewerTourneys(chosenTournaments){ `**
#
### returns games that have had more tournament winners than the given number
#### chosenWinners {number} - a number that the function uses to return games with more tournament winners
#### return {list} - the list of games that have more tournament winners than the given number

**` function getTotalTournamentWinners(chosenWinnners){ `**

### Returns what games have less online money earned than number given
#### chosenWinners{number} a number that the funcuction uses to 
#### return games that have tournements with winners than imput number

**`  function getMoneyEarnedOnline(chosenOnline){ `**

### Returns what games have earn money under the amount given  
#### chosenMoney{number} a number that the funcuction uses to 
#### return games that have less money earned total than imput number

**` function getTotalMoneyEarned(chosenMoney){ `**

### returns what games were released before the the number given
#### chosenDate{number} a number that the funcuction uses to 
#### return games released earlier than year given

**` function getGenre(chosenDate){ `**



